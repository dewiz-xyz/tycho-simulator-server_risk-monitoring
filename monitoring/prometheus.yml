global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers: []

rule_files: []

scrape_configs:
  - job_name: tycho-risk-monitor
    static_configs:
      - targets:
          - localhost:3000
    metrics_path: /metrics
    scrape_interval: 5s
